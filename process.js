console.log(`Node.js version: ${process.version}`); // Logging the version to the console.
console.log(`Current directory: ${process.cwd()}`);
console.log(`Environment variables; ${JSON.stringify(process.env)}`);