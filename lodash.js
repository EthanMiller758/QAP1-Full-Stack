const thing = require('lodash');

const numbers = [1, 2, 3, 4, 5];

const sum = thing.sum(numbers); // This will take the numbers and add them all up.

console.log(`The sum of the numbers is ${sum}`);